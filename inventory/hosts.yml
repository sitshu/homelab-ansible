---
all:
  hosts:
    homelab:
      ansible_connection: local
      ansible_host: localhost
      ansible_python_interpreter: /usr/bin/python3
  vars:
    # See group_vars/all/main.yml for service-level config
